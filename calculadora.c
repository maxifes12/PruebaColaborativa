#include <stdio.h>
#include <stdint.h>

//Realizar las siguientes funciones
float calcula_Perimetro_Cuadrado(float a); //1  
float calcula_Perimetro_Rectangulo(float a,float b); //2 
float calcula_Perimetro_Triangulo(float a,float b, float c); //3
float calcula_Circunferencia(float r); //4
float calcula_Area_Cuadrado(float a); //5
float calcula_Area_Rectangulo(float a, float b); //6
float calcula_Area_Triangulo(float base, float altura); //7 
float calcula_Area_Circulo(float r); //8
float suma_N_Numeros(float *arreglo, int sizeArreglo); //9
float multiplica_N_Numeros(float *arreglod, float sizeArreglo); //10 
float elevar_aPotencia(float base,float potencia); //11



int main(int argc, char const *argv[])
{
    float a;

    printf("%f",a);
    return 0;
}
