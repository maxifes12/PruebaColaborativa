#include <stdio.h>
#include <stdint.h>

float calcula_Perimetro_Cuadrado(float a);
float calcula_Perimetro_Rectangulo(float a,float b); 
float calcula_Perimetro_Triangulo(float a,float b, float c);
float calcula_Circunferencia(float r);  
float calcula_Area_Cuadrado(float a); 
float calcula_Area_Rectangulo(float a, float b);
float calcula_Area_Triangulo(float base, float altura); 
float calcula_Area_Circulo(float r);
float suma_N_Numeros(float *arreglo, int sizeArreglo);
float multiplica_N_Numeros(float *arreglod, float sizeArreglo); 
float elevar_aPotencia(float base,float potencia);



int main(int argc, char const *argv[])
{
    float a;

    printf("%f",a);
    return 0;
}
